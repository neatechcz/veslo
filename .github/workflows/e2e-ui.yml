name: E2E UI Tests

on:
  push:
    branches: [dev]
  pull_request:
    branches: [dev]

permissions:
  contents: read

jobs:
  e2e-ui:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: packages/desktop/src-tauri

      - uses: pnpm/action-setup@v6
        with:
          version: 10.27.0
      - uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install tauri-pilot CLI
        run: cargo install tauri-pilot-cli --version 0.7.2 --locked

      - name: Install Linux system deps
        if: runner.os == 'Linux'
        uses: awalsh128/cache-apt-pkgs-action@latest
        with:
          packages: libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf xvfb
          version: "1.0"

      # Sidecar download — adapt the full platform-aware download logic
      # from build-desktop.yml when enabling this workflow.
      # For now, prepare:sidecar handles what it can.
      - name: Prepare sidecars
        run: pnpm --filter @neatech/veslo run prepare:sidecar
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Tauri (debug, no bundle, e2e feature)
        run: pnpm tauri build --debug --no-bundle --config src-tauri/tauri.e2e.conf.json -- --features e2e
        working-directory: packages/desktop

      - name: Run E2E UI tests (Linux)
        if: runner.os == 'Linux'
        run: xvfb-run -a pnpm test:e2e:ui

      - name: Run E2E UI tests (macOS / Windows)
        if: runner.os != 'Linux'
        run: pnpm test:e2e:ui

      - name: Run terminal assistant ordering gate (Linux)
        if: runner.os == 'Linux'
        run: xvfb-run -a pnpm --filter @neatech/veslo-e2e run test:pilot:session-run-truthfulness

      - name: Run terminal assistant ordering gate (macOS / Windows)
        if: runner.os != 'Linux'
        run: pnpm --filter @neatech/veslo-e2e run test:pilot:session-run-truthfulness

      - name: Upload visual diffs on failure
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: e2e-failure-artifacts-${{ matrix.os }}
          path: |
            packages/e2e/tauri-pilot-failures/**
            packages/e2e/__snapshots__/**/diff/
