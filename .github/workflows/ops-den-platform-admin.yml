name: Ops DEN Platform Admin

on:
  workflow_dispatch:
    inputs:
      email:
        description: DEN user email to grant platform_admin
        required: true
        type: string
      apply:
        description: Apply the grant. Leave false for a dry run.
        required: true
        type: boolean
        default: false

permissions:
  contents: read

concurrency:
  group: ops-den-platform-admin-production
  cancel-in-progress: false

jobs:
  grant:
    runs-on: [self-hosted, linux, x64, veslo-owned-server]
    if: github.repository == 'neatechcz/veslo'
    env:
      OWNED_SERVER_APP_DIR: ${{ vars.OWNED_SERVER_APP_DIR || '/home/neatech/veslo-owned-server-production' }}
      OWNED_SERVER_ENV_FILE: ${{ vars.OWNED_SERVER_ENV_FILE || '/home/neatech/veslo-owned-server-dark-launch-inputs/env/production.env' }}
      TARGET_EMAIL: ${{ inputs.email }}
      APPLY_GRANT: ${{ inputs.apply }}
    steps:
      - name: Validate owned-server ops configuration
        run: |
          set -euo pipefail

          missing=0
          for key in OWNED_SERVER_APP_DIR OWNED_SERVER_ENV_FILE TARGET_EMAIL APPLY_GRANT; do
            if [ -z "${!key}" ]; then
              echo "::error::Missing required owned-server ops value: $key"
              missing=1
            fi
          done

          if [ "$missing" -ne 0 ]; then
            exit 1
          fi

          test -d "$OWNED_SERVER_APP_DIR/.git"
          test -f "$OWNED_SERVER_ENV_FILE"
          test -f "$OWNED_SERVER_APP_DIR/packaging/owned-server/compose.yml"

      - name: Grant platform admin role
        run: |
          set -euo pipefail

          cd "$OWNED_SERVER_APP_DIR"

          compose() {
            sudo -n docker compose -f packaging/owned-server/compose.yml --env-file "$OWNED_SERVER_ENV_FILE" "$@"
          }

          compose exec -T \
            -e TARGET_EMAIL="$TARGET_EMAIL" \
            -e APPLY_GRANT="$APPLY_GRANT" \
            den sh -lc 'cd /app/services/den && node <<'"'"'NODE'"'"'
          const mysql = require("mysql2/promise");
          const crypto = require("node:crypto");

          (async () => {
            const databaseUrl = process.env.DATABASE_URL;
            const targetEmail = (process.env.TARGET_EMAIL || "").trim().toLowerCase();
            const applyGrant = (process.env.APPLY_GRANT || "false").toLowerCase() === "true";

            if (!databaseUrl) {
              throw new Error("DATABASE_URL is required");
            }
            if (!targetEmail || !targetEmail.includes("@")) {
              throw new Error("TARGET_EMAIL must be a valid email");
            }

            const connection = await mysql.createConnection(databaseUrl);
            try {
              const [users] = await connection.execute(
                "SELECT id, email FROM `user` WHERE lower(email) = ? LIMIT 1",
                [targetEmail],
              );
              const user = Array.isArray(users) ? users[0] : null;
              if (!user?.id || !user?.email) {
                throw new Error(`user_not_found:${targetEmail}`);
              }

              const [existingRows] = await connection.execute(
                "SELECT id, role FROM platform_role WHERE user_id = ? AND role = 'platform_admin' LIMIT 1",
                [user.id],
              );
              const existing = Array.isArray(existingRows) ? existingRows[0] : null;

              if (applyGrant && !existing) {
                await connection.execute(
                  "INSERT INTO platform_role (id, user_id, role, created_at) VALUES (?, ?, 'platform_admin', CURRENT_TIMESTAMP(3)) ON DUPLICATE KEY UPDATE role = 'platform_admin'",
                  [`platform_role_${crypto.randomUUID()}`, user.id],
                );
              }

              const [afterRows] = await connection.execute(
                "SELECT id, role FROM platform_role WHERE user_id = ? AND role = 'platform_admin' LIMIT 1",
                [user.id],
              );
              const after = Array.isArray(afterRows) ? afterRows[0] : null;

              console.log(JSON.stringify({
                email: user.email,
                userId: user.id,
                hadPlatformAdminRole: Boolean(existing),
                applied: applyGrant,
                hasPlatformAdminRole: Boolean(after),
              }));
            } finally {
              await connection.end();
            }
          })().catch((error) => {
            console.error(error instanceof Error ? error.message : String(error));
            process.exitCode = 1;
          });
          NODE'
