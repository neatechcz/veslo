---
title: "Development"
description: "Develop and publish this docs site from the Veslo monorepo"
---

## Docs source

Docs live at `packages/docs` in `neatechcz/veslo`.

## Run locally

```bash
npm i -g mint
cd packages/docs
mint dev
```

## Validate docs

```bash
mint broken-links
```

## Typical workflow

1. Edit `.mdx` pages or `docs.json` in `packages/docs`.
2. Validate with local preview and `mint broken-links`.
3. Push to `neatechcz/veslo`.
4. Confirm Mintlify Git settings point to:
   - repo: `neatechcz/veslo`
   - monorepo path: `/packages/docs`
