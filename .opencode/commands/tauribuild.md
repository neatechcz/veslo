---
description: Build the full Veslo app with Tauri Pilot enabled
---

You are running the Veslo full-app Tauri Pilot build flow in this repo.

Arguments: `$ARGUMENTS`

Do the following, in order:

1. Read `.agents/skills/tauribuild/SKILL.md`.
2. Run `.agents/skills/tauribuild/scripts/tauribuild.sh $ARGUMENTS`.
3. Do not start a UI-only web server.
4. Report the app, DMG, updater tarball, Pilot verification status, and whether repo files were restored.
