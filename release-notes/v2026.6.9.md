## What's Changed

- Promotes the current desktop build to a stable release after macOS signing and notarization setup was completed.
- Publishes signed and notarized macOS Apple Silicon and Intel artifacts for smoother installation on new Macs.
- Keeps the Windows installer available alongside the refreshed macOS release artifacts.

## Update Notes

- macOS users should be able to open this build without the unsigned-app Gatekeeper warning that appeared on earlier test builds.
