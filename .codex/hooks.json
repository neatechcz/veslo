{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "sh -lc 'command -v graphify >/dev/null 2>&1 && graphify hook-check || true'"
          }
        ]
      }
    ]
  }
}
